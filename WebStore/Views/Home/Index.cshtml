

@await Html.PartialAsync("Partial/_Slider") @*или таг-хелпер <partial name = "Partial/_Slider>*@

<section>
    <div class="container">
        <div class="row">
            <div class="col-sm-3">
                @await Html.PartialAsync("Partial/_LeftSidebar")
            </div>

            <div class="col-sm-9 padding-right">
                @await Html.PartialAsync("Partial/_FeaturesItem")

                @await Html.PartialAsync("Partial/_CategoryTab")

                @await Html.PartialAsync("Partial/_RecommendedItems")
            </div>
        </div>
    </div>
</section>

